-- =====================================================================
-- check_ins.pairing — 함께 마신 음식 (자유 텍스트 한 줄)
-- 이유: 메모(note)와 분리해 *집계* 가능한 데이터 자산으로. 향후 제품 페이지에
--      "함께 마신 음식 TOP 5" 같은 추천에 활용. Phase 2 의 페어링 추천 베이스.
-- 폼: 선택 입력. 빈 값 → null.
-- =====================================================================

alter table public.check_ins
  add column if not exists pairing text;
