{
  "name": "sooly-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Sooly · ETL & one-off scripts (separate from web/ to isolate deps)",
  "type": "module",
  "scripts": {
    "import:products": "tsx import-products.ts",
    "import:products:dry": "tsx import-products.ts --dry-run"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "xlsx": "^0.18.5"
  }
}
