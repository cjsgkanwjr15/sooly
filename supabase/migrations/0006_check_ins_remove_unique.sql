-- =====================================================================
-- check_ins (user_id, product_id) unique 제거 — 여러 체크인 허용
-- 이유: 0005 에서 1인 1제품 1체크인 정책으로 갔다가 사용자 피드백으로 정정.
--      Untappd 스타일: 같은 술 다른 술자리 = 다른 별점·다른 메모, 시간순 누적.
--      체크인 자체가 자산이라 누적될수록 데이터 가치 ↑.
-- 영향: 기존 row 그대로 살아있음 (제약만 풀림). upsert → insert 로 코드도 변경.
-- =====================================================================

alter table public.check_ins
  drop constraint if exists check_ins_user_product_unique;
